import XCTest
@testable import VersaPlayerAirplayExtension

final class VersaPlayerAirplayExtensionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VersaPlayerAirplayExtension().text, "Hello, World!")
    }
}
